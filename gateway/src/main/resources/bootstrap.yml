spring:
  profiles:
    active: @configProfile@
  application:
    name: gateway,client-monitor
  cloud:
    config:
      uri: @configUri@
      fail-fast: true
      password: 6mS9WQ2M
      username: user
      profile: @configProfile@




