spring:
  profiles:
    active: @configProfile@
  application:
    name: monitor
  cloud:
    config:
      uri: @configUri@
      fail-fast: true
      password: 6mS9WQ2M
      username: user
      profile: @configProfile@
      name: monitor-server-admin-eureka,monitor-server,monitor-server-zipkin


