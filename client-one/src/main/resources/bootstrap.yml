
spring:
  profiles:
    active: @configProfile@
  application:
    name: client-one
  cloud:
    config:
      name: client-one,client-hystixs,client-monitor,client-tkmapper
      uri: @configUri@
      fail-fast: true
      password: 6mS9WQ2M
      username: user
      profile: @configProfile@
