spring:
  profiles:
    active: dev
  application:
    name: service-a
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: 6mS9WQ2M
      username: user
      profile: dev

management:
  security:
    # 仅限于 开发环境可对security进行关闭。
    enabled: false
  metrics:
    export:
      prometheus:
        enabled: true
        descriptions: true

#tk.mapper配置
mapper:
  mappers: com.example.servicea.core.Mapper
  not-empty: true
  identity: mysql
