spring:

  profiles:
    active: @configProfile@
  application:
    name: zipkin
  cloud:
    config:
      uri: @configUri@
      fail-fast: true
      password: 6mS9WQ2M
      username: user
      profile: @configProfile@
      name: zipkin

management:
  security:
    enabled: false
zipkin:
  storage:

